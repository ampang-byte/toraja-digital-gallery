/* WARNA DASAR */
:root {
    --theme: #fdfaf5;
    --primary: #2c3e50;
    --entry: #ffffff;
    --tertiary: #8b0000;
}

/* --- TRIK BANNER PENUH (HERO IMAGE) --- */

/* 1. Membuat Wadah Header Menjadi Background Gambar */
.profile_inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 500px; /* Tinggi Banner */
    width: 100%;
    margin: 0;
    padding: 40px;
    background-image: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url("/header-toraja.jpg");
    background-size: cover;
    background-position: center;
    border-radius: 15px;
    color: #ffffff !important; /* Tulisan di dalam banner jadi putih */
    box-shadow: 0 10px 30px rgba(0,0,0,0.2);
}

/* 2. Menyembunyikan Gambar Bulat Bawaan (Karena sudah jadi background) */
.profile_inner img {
    display: none;
}

/* 3. Membuat Teks di Tengah Banner Jadi Putih & Jelas */
.profile_inner .profile_description, .profile_inner h1 {
    color: #ffffff !important;
    text-shadow: 2px 2px 8px rgba(0,0,0,0.8); /* Bayangan teks agar terbaca */
}

.profile_inner h1 {
    font-size: 3rem;
    margin-bottom: 10px;
}

/* 4. Navbar Harus Tetap di Atas (Gaya Transparan) */
.header {
    background: transparent;
    position: relative;
    z-index: 100;
}

/* Agar Menu Navigasi tetap mudah dibaca */
.menu a {
    font-weight: bold;
    color: var(--primary);
}

/* Tombol di dalam Banner */
.profile_inner .buttons .button {
    background: #8b0000;
    color: white !important;
    border: none;
}

.profile_inner .buttons .button:hover {
    background: #600000;
}